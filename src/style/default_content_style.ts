import { SxProps } from "@mui/material"

export const default_content_wrapper_style: SxProps = { flexDirection: "column", width: 0.8, padding: 3, gap: 3, overflow: "hidden" }

export const default_content_list_style: SxProps = {
    flexDirection: "column",
    // padding: 3,
    height: 1,
    overflowY: "auto",
}
